[tool.ruff]
target-version = "py39"
line-length = 100
extend-exclude = ["old", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"run*.py" = ["F401", "I001"]
"run_template.py" = ["F401", "I001"]
