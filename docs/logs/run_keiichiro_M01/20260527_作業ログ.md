# 作業ログ: run_keiichiro_M01.py

**日付**: 2026-05-27  
**担当**: Keiichiro  
**ファイル**: `run_keiichiro_M01.py`

---

## 変更概要

`robot.straight(300)` の直後に `await wait(3000)` を追加し、300mm 前進後に3秒間待機するようにした。

---

## 変更前のコード

```python
async def run(hub, robot, left_wheel, right_wheel, left_lift, right_lift):
    await robot.straight(300)
    await robot.straight(-150, speed=200)
    await robot.straight(50)
    await robot.turn(-90)
    await robot.straight(-100)
    await right_lift.run_angle(1000, -720)
    await robot.straight(90)
    await right_lift.run_angle(1000, 720)
```

---

## 変更後のコード

```python
async def run(hub, robot, left_wheel, right_wheel, left_lift, right_lift):
    await robot.straight(300)
    await wait(3000)
    await robot.straight(-150, speed=200)
    await robot.straight(50)
    await robot.turn(-90)
    await robot.straight(-100)
    await right_lift.run_angle(1000, -720)
    await robot.straight(90)
    await right_lift.run_angle(1000, 720)
```

---

## 変更詳細

| # | 変更内容 | 変更前 | 変更後 |
|---|---|---|---|
| 1 | 300mm 前進後に3秒待機を追加 | （なし） | `await wait(3000)` |

---

## 動作シーケンス（変更後）

1. **300mm 前進**
2. **3秒 待機** ← 今回追加
3. **150mm 後退**（速度 200mm/s）
4. **50mm 前進**
5. **左90度 旋回**
6. **100mm 後退**
7. **右アーム**: 1000deg/s で 720度逆転
8. **90mm 前進**
9. **右アーム**: 1000deg/s で 720度正転

---

## 備考

- `wait(3000)` はミリ秒指定のため 3000 = 3秒。
