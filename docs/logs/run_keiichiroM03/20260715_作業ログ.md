# 作業ログ: run_keiichiroM03.py

**日付**: 2026-07-15  
**担当**: Keiichiro  
**ファイル**: `run_keiichiroM03.py`

---

## 変更概要

右リフトモーター（right_lift）を上方向に1回転（360度）させる動作を追加した。

---

## 変更前のコード

```python
    #######################################
    # ここにロボットの動作を記述してください
    #######################################

    # ロボットを停止
    robot.stop()
    print("# 走行完了！")
```

---

## 変更後のコード

```python
    #######################################
    # ここにロボットの動作を記述してください
    #######################################

    # 右リフトモーターを上に1回転（360度、速度300deg/s）
    await right_lift.run_angle(300, 360)

    # ロボットを停止
    robot.stop()
    print("# 走行完了！")
```

---

## 変更詳細

| # | 変更内容 | 変更前 | 変更後 |
|---|---|---|---|
| 1 | 右リフト回転の追加 | （なし） | `await right_lift.run_angle(300, 360)` |

---

## 動作シーケンス（変更後）

1. **右リフトモーターを上に1回転**（速度300deg/s、角度360度）
2. **停止**

---

## 備考

- `right_lift` の `positive_direction` は `CLOCKWISE`（時計回り）
- `+360` で上方向への1回転。もし逆方向になる場合は `-360` に変更する
- 速度（300deg/s）は必要に応じて調整可能
