# 作業ログ: run_0711.py

**日付**: 2026-07-11  
**担当**: —  
**ファイル**: `run_0711.py`

---

## 変更概要

`selector.py` のセンサーログ機能（`sensor_logger_task` と `dev` フラグ）を `run_0711.py` に移植し、単体テスト時にも開発モードでセンサーログを取得できるようにした。

---

## 変更前のコード

```python
# ===== 単体テスト用（このファイルを直接実行した場合） =====
if __name__ == "__main__":
    hub, robot, left_wheel, right_wheel, left_lift, right_lift = initialize_robot()
    run_task(run(hub, robot, left_wheel, right_wheel, left_lift, right_lift))
```

---

## 変更後のコード

```python
# ★★★ここを変更することで、開発モードと本番モードを切り替えます★★★
dev = True  # True=開発モード（センサーログ有効）、False=本番モード


async def sensor_logger_task(hub, robot, left_wheel, right_wheel):
    """0.2秒ごとに dist / heading / L / R をログ出力するタスク"""
    print("--- センサーログタスク開始 ---")
    logger_timer = StopWatch()
    logger_timer.reset()
    while True:
        elapsed_time = logger_timer.time()
        heading = hub.imu.heading()
        left_deg = left_wheel.angle()
        right_deg = right_wheel.angle()
        dist = robot.distance()
        print(
            f"LOG[{elapsed_time:5.0f}ms]: dist={dist:4.0f} mm  heading={heading:4.0f}°  L={left_deg:5.0f}°  R={right_deg:5.0f}°"
        )
        await wait(200)


# ===== 単体テスト用（このファイルを直接実行した場合） =====
if __name__ == "__main__":
    hub, robot, left_wheel, right_wheel, left_lift, right_lift = initialize_robot()

    if dev:
        print("--- 開発モードで起動（センサーログ有効） ---")
        run_task(
            multitask(
                sensor_logger_task(hub, robot, left_wheel, right_wheel),
                run(hub, robot, left_wheel, right_wheel, left_lift, right_lift),
            )
        )
    else:
        print("--- 本番モードで起動（センサーログなし） ---")
        run_task(run(hub, robot, left_wheel, right_wheel, left_lift, right_lift))
```

---

## 変更詳細

| # | 変更内容 | 変更前 | 変更後 |
|---|---|---|---|
| 1 | dev フラグを追加 | なし | `dev = True` をモジュールトップレベルに追加 |
| 2 | sensor_logger_task を追加 | なし | `async def sensor_logger_task(hub, robot, left_wheel, right_wheel)` を追加 |
| 3 | __main__ ブロックを dev 対応に変更 | `run_task(run(...))` のみ | dev=True 時に `multitask` でログと run を並行実行 |

---

## 動作シーケンス（変更後・dev=True）

1. ロボット初期化
2. `sensor_logger_task` と `run` を `multitask` で並行起動
3. `sensor_logger_task`: 0.2秒ごとに dist / heading / L / R を出力
4. `run`: ロボットの動作を実行
5. `run` 完了後、`multitask` 終了

---

## 備考

- `sensor_logger_task` は `selector.py` からほぼそのまま移植。グローバル変数依存をなくすため引数渡しに変更した。
- 競技本番では `dev = False` に切り替えることでログなし動作になる。
